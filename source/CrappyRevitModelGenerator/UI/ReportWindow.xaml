<Window x:Class="CrappyRevitModelGenerator.UI.ReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Crappy Revit Model Generator"
        Width="800" Height="600" MinWidth="520" MinHeight="360"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="CanResize"
        UseLayoutRounding="True" TextOptions.TextFormattingMode="Display">
    <Window.Resources>
        <Style x:Key="MonoText" TargetType="TextBox">
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="IsReadOnlyCaretVisible" Value="True"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="FontFamily" Value="Consolas, Courier New, monospace"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="6"/>
        </Style>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="MinWidth" Value="110"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock x:Name="HeadlineText" FontSize="14" FontWeight="SemiBold" TextWrapping="Wrap"/>
            <TextBlock x:Name="SummaryText" TextWrapping="Wrap" Margin="0,2,0,0" Foreground="Gray"/>
        </StackPanel>

        <TabControl x:Name="Tabs" Grid.Row="1">
            <TabItem x:Name="TextTab" Header="Report">
                <TextBox x:Name="TextView" Style="{StaticResource MonoText}"/>
            </TabItem>
            <TabItem x:Name="JsonTab" Header="JSON">
                <TextBox x:Name="JsonView" Style="{StaticResource MonoText}"/>
            </TabItem>
        </TabControl>

        <Grid Grid.Row="2" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText" Grid.Column="0" Foreground="Gray" VerticalAlignment="Center"
                       TextTrimming="CharacterEllipsis" Margin="0,0,8,0"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Copy to clipboard" Click="OnCopy" Style="{StaticResource ActionButton}"/>
                <Button x:Name="ExportButton" Content="Export JSON…" Click="OnExport" Style="{StaticResource ActionButton}"/>
                <Button Content="Close" IsCancel="True" IsDefault="True" Style="{StaticResource ActionButton}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
